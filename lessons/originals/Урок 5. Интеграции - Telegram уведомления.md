К этому уроку у нас получился работающий проект - лендинг собирает лиды, мы имеем возможность увидеть эффективность его работы через подсчет конверсий, также можем выстраивать взаимодействие с внешними системами.

Не хватает только немного увеличить наблюдаемости - быстрее реагировать на входящие лиды и сразу начинать с ними работать, пока они еще "теплые". Для этого можно реализовать уведомления по определенным событиям, например - при создании нового лида отправлять сообщения в Telegram.

Что нам для этого потребуется:
- создать Telegram бота в специальном боте BotFather
- создать чат с этим ботом, чтобы иметь возможность получать от него сообщения
- интегрировать нашу экосистему лендинга с ботом и настроить ее
- настроить формат отправляемых сообщений

Для начала перейдем в Telegram, найдем бота BotFather и попросим его создать нам бота под нужды лендинга, например: 
- имя - Landing Bot
- ник - ya_land_bot

В результате создания станет доступным для копирования токен бота.
Далее перейдем в бота и запустим его командой `/start` и  отправьте произвольное сообщение, после чего у бота появится возможность писать нам сообщения.

Попросим агента интегрироваться с вновь созданным ботом, вместо `<token>` нужно вставить токен бота:
```
Ты опытный devops-инженер с навыками backend-разработчика.

Необходимо настроить интеграцию с Telegram-ботом для отправки сообщений по событиям из LogEvent.

Задачи:
- доработать docker-compose добавить в сервис app переменную TG_TOKEN=<token> 
- выполнить запрос GET https://api.telegram.org/bot<TG_TOKEN>/getUpdates, из ответа получить chat.id и записать его в переменную TG_CHAT_ID сервиса app
- при срабатывании события lead_created необходимо отправлять сообщение через бота TG_TOKEN в TG_CHAT_ID, в сообщении передавать информацию по созданному лиду
- перезапустить docker compose окружение 
- добавивить git коммит
```

Теперь заходим на лендинг, отправляем форму с лидом и моментально получаем сообщения от Telegram-бота!

Примечание: при желании можно попросить агента выделить шаблоны сообщений Telegram в отдельные файлы в специальной папке проекта и управлять форматом отправляемого сообщения

<проверка знаний>